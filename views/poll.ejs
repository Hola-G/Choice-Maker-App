<!DOCTYPE html>

<html lang="en">

<% include ./partials/_header %>
<head>
<meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
  #sortable {
    list-style-type: none;
    margin: 0;
    padding: 0;
    width: 60%;
  }
  #sortable li {
    margin: 0 3px 3px 3px;
    padding: 0.4em;
    padding-left: 1.5em;
    font-size: 1.4em;
    height: auto;
  }
  #sortable li span {
    position: absolute;
    margin-left: -1.3em;
  }
  .col-centered {
    float: none;
    margin: 0 auto;
  }
  </style>
  <script src="https://code.jquery.com/jquery-1.12.4.js"></script>
  <script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>
  <script>
  $( function() {
    $( "#sortable" ).sortable();
    $( "#sortable" ).disableSelection();
  } );
  </script>
</head>

<body>
  <div class="row">
    <div class="col-lg-12 col-centered">
      <h1><%= poll.poll_title %></h1>
      <form method="POST" action="/poll">
      <ul id="sortable">
        <% for (key in poll.options) { %>
        <li class="ui-state-default" rel="option<%= key %>">
          <h4 class="card-header"><%= poll.options[key].option_name %></h4>
          <p class="card-text"><%= poll.options[key].option_desc %></p>
        </li>
        <% } %>
      </ul>
      <button class="btn btn-info submitoptions">Done</button>
    </form>
    </div>
  </div>
</body>
</html>
